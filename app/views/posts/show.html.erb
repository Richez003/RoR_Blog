</section>
    <div class = "Postpage" >
        <div  class = "page">
        <p>Post #1 by<%= @user.name %></p>
         <p class = "postsCounter" >
            Comments: <%= @posts.comments_counter %>
            Likes:<%= @user.likes.size %>
        </p>

        </div>
        <%= @posts.text %>

    </div>

<div class = "usercomments" >
       <% @posts.five_recent_comments.each do |comment| %>
      <span><%= comment.user.name %>: <%= comment.text %></span>
    <%end%>
</div>
</section>


